package com.learning.bank.customer.customerservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
	/*private ApiInfo apiInfo() {
		return new ApiInfo("Customer REST APIs", "REST APIs for Customer Application",
				"1.0.0", "Terms of service", new Contact("Saurabh Singh",
						"www.javaguides.net", "saurabh.singh5788@gmail.com"),
				"License of API", "API license URL", Collections.emptyList());
	}

	@Bean
	public Docket api() {
		return new Docket(DocumentationType.SWAGGER_2).apiInfo(apiInfo())
				.select().apis(RequestHandlerSelectors.any())
				.paths(PathSelectors.any()).build();
	}*/
}
